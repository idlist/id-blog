---
title: Test
route: test
date: 2021-10-01
tags: test ipsum_lorem
layout: article
---

# Test

It's a test page.

Inline code: `const a = (x) => { return x ? true : false }`

```js
import { createHash } from 'crypto'

let x = new Prmoise((resolve, reject) => {
  timeout(() => { resolve() }, 1000)
})

const add = (a, b, sign) => {
  let sum = a + b
  if (sign > 0) {
    return a + b // line comment
  } else {
    return a - b
  }
}
```

```sass
@import ../layouts-components/constants
@import ../layouts-components/vscode.css

.article-container
  padding: 1rem
```