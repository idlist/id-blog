---
import type { GetStaticPaths } from 'astro'
import Base from '@/layout/base.astro'
import { mapRoute } from '@/i18n'

export const getStaticPaths = (() => {
  return [
    ...mapRoute()
  ]
}) satisfies GetStaticPaths
---

<Base title="i'D Blog">Test</Base>